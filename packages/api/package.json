{
    "name": "@scriptscope/api",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "cargo run",
        "build": "cargo build --release",
        "start": "cargo run --release",
        "test": "cargo test",
        "lint": "echo \"lint not configured\"",
        "typecheck": "echo \"Rust typecheck done via compiler\""
    }
}
